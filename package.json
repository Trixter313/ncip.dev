{
  "name": "cip.dev",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "rm -rf dist && rollup -c dev-rollup.config.mjs -w",
    "build": "rm -rf dist && rollup -c",
    "web": "web-dev-server --node-resolve --open --watch --root-dir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@material/web": "^1.0.0",
    "@shoelace-style/shoelace": "^2.9.0",
    "lit": "^2.8.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.1",
    "@web/dev-server": "^0.3.1",
    "cssnano": "^6.0.1",
    "html-minifier": "^4.0.0",
    "postcss": "^8.4.31",
    "rollup-plugin-copy": "^3.5.0",
    "rollup-plugin-terser": "^7.0.2"
  }
}
